name: CI

on:
  push:
    branches:
      # test branches
      - 'try-**'

      # main branches
      - main
      - next
  pull_request:
  release:
    types: [released, pre-released]

jobs:
  ci:
    strategy:
      matrix:
        version:
          - 1.47.0 # MSRV
          - stable
          - nightly
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v2

      - name: setup toolchain
        # `--no-self-update` is needed due to a permission issue on the GHA env.
        run: |
          rustup set profile minimal
          rustup toolchain install ${{ matrix.version }} --no-self-update
          rustup override set ${{ matrix.version }}

      - name: check build
        if: matrix.version != 'stable'
        run: cargo check --features serde --examples

      - name: check build hot reload tide
        if: matrix.version != 'stable'
        run: cargo check --manifest-path examples/hot_reload_tide/Cargo.toml

      - name: test
        if: matrix.version == 'stable'
        shell: bash
        run: tests/and-retry --features serde

  audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: install cargo-audit
        run: |
          rustup override set stable
          cargo install cargo-audit

      - name: audit check
        run: cargo audit
